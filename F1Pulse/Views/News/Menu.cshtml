@model F1Pulse.Models.News.MenuModel

<div class="page_content">
    <form method="post">
        <div class="form_search">
            <div class="input-group">
                <input class="form-control" asp-for="Day"/>
                <input class="form-control" asp-for="Month"/>
                <input class="form-control" asp-for="Year"/>
                <button class="btn-danger" type="submit">Поиск</button>
            </div>
        </div>
    </form>
    @foreach (var news in Model.NewsList)
    {
        
        <div class="article shadow">
            <div class="article_image">
                <a asp-action="Article" asp-route-id="@news.FullContentLink.Split(new char[] { '-', '.' })[3]" class="b-article__figure">
                    <img src=@news.ImageLink alt=@news.Preview class="b-article__image"/>
                </a>
            </div>
            <div class="article_main">
                <div class="article_title">
                    <a asp-action="Article" asp-route-id="@news.FullContentLink.Split(new char[] { '-', '.' })[3]">
                        <span>@news.PostDate</span>
                        @news.Title
                    </a>
                </div>
                <div class="article_content">
                    @news.Preview
                </div>
            </div>
        </div>
    
    }
</div>

@section Scripts
{
    <link rel="stylesheet" href="~/css/News/Menu.css"/>
}
